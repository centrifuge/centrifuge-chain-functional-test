// This is just for ref, delete later
{
  "extends": "./node_modules/@polkadot/dev/config/tsconfig",
  "compilerOptions": {
    "baseUrl": ".",
    "typeRoots": [
      "./node_modules/@polkadot/ts",
      "./node_modules/@types"
    ],
    // FIXME This is a HACK that is needed for the current mock-socket defs
    "skipLibCheck": true
  }
}